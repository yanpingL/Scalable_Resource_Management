import { RegisterForm } from "@/features/auth/components/RegisterForm";

// Shows the registration route by rendering the register form.
export default function RegisterPage() {
  return <RegisterForm />;
}
